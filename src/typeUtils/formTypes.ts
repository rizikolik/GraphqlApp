export type onSubmitForms = (token: string) => void;

export type LoginAreas = {
  email: string;
  password: string;
};
export type SignupAreas = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};
